import styled from "styled-components";

const StyledCredits = styled.span`
  margin-top: auto;
  font-weight: 700;
  color: #c4c4c4;
`;

export default function Credits() {
  return <StyledCredits>Powered by CHOWIS</StyledCredits>;
}
